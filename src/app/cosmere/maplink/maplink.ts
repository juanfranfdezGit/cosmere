import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-maplink',
  imports: [RouterLink],
  templateUrl: './maplink.html',
  styleUrl: './maplink.scss',
})
export class Maplink {

}
